server:
  port: 3000

dir:
  static: public
  controller: lib/www/controller
  save: public/static/data
  log: log
  presenter:
    view:
      include: include
      template: template

file:
  log:
    access: access.log
    error: error.log

uri:
  static: /static

default:
  encoding: utf8
